# Server Configuration
server.port=${SERVER_PORT:8085}
spring.application.name=order_service

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-crimson-fog-a8ytgnvx-pooler.eastus2.azure.neon.tech:5432/orderdb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_PX61MuCJGEtD}
spring.datasource.driver-class-name=${DB_DRIVER:org.postgresql.Driver}
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}

# Service URLs
gig.service.url=${GIG_SERVICE_URL:http://localhost:8084}

# Cloudinary Configuration
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:drgaoywmi}
cloudinary.api_key=${CLOUDINARY_API_KEY:115764739691513}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:ptG4dMITrCTOQr6jshY8HPHW6Po}

# File Upload Configuration
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:50MB}
spring.servlet.multipart.enabled=${MULTIPART_ENABLED:true}
